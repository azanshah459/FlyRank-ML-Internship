# Data files are regenerated by the notebooks on every run.
# The CI leak-guard blocks data files from git; keep only the JSON receipts.
work/outputs/*.csv
work/outputs/*.parquet
work/outputs/*.tsv

# Office temp / lock files
~$*
*.tmp

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
